<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unbelievable.nothero">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.WelcomeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomeTabActivity"
            android:label="@string/act_lable_tab" />
        <activity
            android:name=".activity.BarChartActivity"
            android:label="@string/act_lable_bar_chart" />
        <activity
            android:name=".testadapter.AdapterActivity"
            android:label="@string/act_lable_adapter" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".activity.SimpleSelectPhotoActivity"
            android:label="@string/act_lable_simple_select_photo" />
        <activity android:name=".app.FragmentBuilderActivity"
            android:label="@string/act_lable_app_fragment_builder"></activity>

    </application>

</manifest>